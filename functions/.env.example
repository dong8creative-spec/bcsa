# 포트원 결제 (백엔드)
# PORTONE_API_KEY=
# PORTONE_API_SECRET=

# 카카오 로그인 (백엔드 콜백) — Cloud Run 변수로 동일 이름 설정
# 카카오 개발자 콘솔 > 앱 키 > REST API 키 (JavaScript 키 아님)
# KAKAO_REST_API_KEY=
# 또는 KAKAO_JS_KEY= (같은 앱이면 동일 값일 수 있음)
# 선택: 클라이언트 시크릿 사용 시
# KAKAO_CLIENT_SECRET=
# 콜백 후 리다이렉트할 프론트 주소
# FRONTEND_URL=https://bcsa.co.kr
# 선택: 콜백 base URL 고정 시 (미설정 시 req host 사용)
# KAKAO_REDIRECT_BASE_URL=https://apibid-oytjv32jna-du.a.run.app

# 부산청년사업가들 웹사이트 구조 분석

## 📁 프로젝트 구조 개요

### 현재 구조

```
bcsa/
├── assets/                    # 정적 리소스 (루트)
│   ├── css/
│   ├── images/
│   └── js/
├── public/                    # Vite public 디렉토리
│   ├── admin.html            # 관리자 페이지 (순수 HTML/JS)
│   └── assets/               # 정적 리소스 (중복)
│       ├── css/
│       ├── images/
│       └── js/
├── src/                      # React/Vite 메인 애플리케이션
│   ├── App.jsx              # 메인 React 앱 (11,000+ 줄)
│   ├── main.jsx             # React 엔트리 포인트
│   ├── components/
│   │   └── CustomIcons.jsx
│   ├── services/
│   │   ├── authService.js
│   │   └── firebaseService.js
│   ├── config.js
│   ├── firebase.js          # Firebase v9+ 설정
│   ├── utils.js
│   ├── index.css            # Tailwind CSS
│   └── input.css            # Tailwind CSS (중복)
├── functions/               # Firebase Functions
├── index.html              # React 앱 엔트리 HTML
├── firebase.json           # Firebase 호스팅 설정
├── vite.config.js          # Vite 빌드 설정
└── package.json
```

## ⚠️ 발견된 문제점

### 1. **중복된 디렉토리 구조**
   - ❌ `assets/` 폴더와 `public/assets/` 폴더가 중복
   - ❌ `src/index.css`와 `src/input.css`가 동일한 내용 (Tailwind CSS 중복)
   - 📝 **영향**: 파일 관리 혼란, 불필요한 중복

### 2. **혼재된 빌드 시스템**
   - ✅ React/Vite 앱 (`index.html` → `src/main.jsx` → `App.jsx`)
   - ✅ 순수 HTML/JS 관리자 페이지 (`public/admin.html`)
   - ⚠️ **문제**: 
     - Firebase Hosting이 루트 `.` 디렉토리를 public으로 설정
     - Vite 빌드는 `dist/` 디렉토리에 생성
     - 빌드 아티팩트와 소스 파일이 섞임

### 3. **Firebase 설정 중복**
   - ✅ Firebase v9+ 모듈 방식: `src/firebase.js` (React 앱용)
   - ✅ Firebase v8 SDK 방식: `public/assets/js/firebase-config.js` (admin.html용)
   - 📝 **현황**: 두 가지 방식이 공존 (기술 부채)

### 4. **빌드 프로세스 불일치**
   - 📦 `vite build` → `dist/` 폴더 생성
   - 📦 `build:css` → `assets/css/tailwind.css` 생성 (수동 빌드)
   - ⚠️ **문제**: 
     - Firebase Hosting은 루트 디렉토리 사용
     - Vite 빌드 결과(`dist/`)와 일치하지 않음
     - Tailwind CSS를 수동으로 빌드해야 함

### 5. **파일 크기 문제**
   - ⚠️ `src/App.jsx`: 11,000+ 줄 (매우 큰 단일 파일)
   - ⚠️ `public/admin.html`: 6,000+ 줄 (매우 큰 단일 파일)
   - 📝 **영향**: 유지보수 어려움, 성능 이슈 가능

### 6. **배포 설정 혼란**
   - 🔄 GitHub Actions: `dist/` 폴더를 GitHub Pages에 배포
   - 🔄 Firebase Hosting: 루트 `.` 디렉토리를 배포
   - ⚠️ **문제**: 두 가지 배포 경로가 상충

## ✅ 현재 구조의 장점

1. **명확한 역할 분리**
   - React 앱: 메인 웹사이트
   - admin.html: 독립적인 관리자 대시보드

2. **Firebase 통합**
   - Firestore, Auth, Storage 모두 활용
   - Firebase Functions 지원

3. **모던 개발 스택**
   - Vite (빠른 개발 서버)
   - React 18
   - Tailwind CSS

## 🔧 권장 개선 사항

### 우선순위 높음

1. **빌드 디렉토리 통일**
   - Firebase Hosting의 `public`을 `dist`로 변경
   - Vite 빌드 결과를 직접 배포

2. **중복 파일 정리**
   - `assets/` 폴더 제거 (또는 `public/assets/`로 통합)
   - `src/input.css` 제거

3. **App.jsx 분리**
   - 컴포넌트를 별도 파일로 분리
   - 페이지 단위로 구조화

### 우선순위 중간

4. **Firebase 버전 통일**
   - admin.html도 Firebase v9+로 마이그레이션 고려

5. **빌드 프로세스 자동화**
   - Tailwind CSS를 Vite PostCSS 플러그인으로 통합
   - 단일 빌드 명령으로 모든 리소스 생성

### 우선순위 낮음

6. **프로젝트 구조 리팩토링**
   - `src/pages/`, `src/components/`, `src/hooks/` 등으로 구조화

## 📋 현재 빌드/배포 프로세스

### 개발 환경
```bash
npm run dev              # Vite 개발 서버 (포트 3000)
npm run watch:css        # Tailwind CSS 감시 모드
```

### 프로덕션 빌드
```bash
npm run build            # Vite 빌드 → dist/
npm run build:css        # Tailwind CSS 빌드 → assets/css/tailwind.css
```

### 배포
```bash
# Firebase Hosting
npm run deploy:hosting   # 루트 디렉토리 배포

# GitHub Pages (자동)
# GitHub Actions가 dist/ 폴더를 배포
```

## 🎯 권장 구조 (제안)

```
bcsa/
├── public/                    # 정적 파일 (빌드 전)
│   ├── assets/
│   │   ├── images/
│   │   └── favicon.svg
│   └── admin.html            # 관리자 페이지
├── src/                      # React 앱 소스
│   ├── pages/               # 페이지 컴포넌트
│   ├── components/          # 재사용 컴포넌트
│   ├── services/            # API 서비스
│   ├── hooks/               # 커스텀 훅
│   ├── utils/               # 유틸리티
│   ├── config.js
│   ├── firebase.js
│   ├── App.jsx
│   ├── main.jsx
│   └── index.css
├── dist/                     # 빌드 결과 (자동 생성)
├── functions/               # Firebase Functions
└── index.html              # 엔트리 HTML
```

## ❓ 확인 필요한 사항

1. **배포 전략**
   - Firebase Hosting만 사용할지
   - GitHub Pages도 계속 사용할지

2. **assets 폴더 위치**
   - `assets/`와 `public/assets/` 중 어느 것을 사용하는지
   - 현재 어느 것이 실제로 서비스되는지

3. **Tailwind CSS 빌드**
   - `build:css` 스크립트가 실제로 필요한지
   - Vite가 자동으로 처리할 수 있는지

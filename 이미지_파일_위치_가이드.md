# 이미지 파일 위치 및 이름 가이드

## 📍 필수 이미지 파일 위치

깃허브 저장소에 다음 이미지들을 **정확한 위치와 이름**으로 넣어야 합니다.

---

## 🎯 1. 파비콘 (Favicon) - 필수

### 깃허브 위치
```
저장소 루트/
└── assets/
    └── images/
        ├── favicon.ico              ← 여기에!
        ├── favicon-32x32.png         ← 여기에!
        ├── favicon-16x16.png         ← 여기에!
        └── apple-touch-icon.png      ← 여기에!
```

### 파일명 (정확히 이 이름으로!)
- `favicon.ico` (메인 파비콘, 16x16과 32x32 포함)
- `favicon-32x32.png` (32x32 픽셀)
- `favicon-16x16.png` (16x16 픽셀)
- `apple-touch-icon.png` (180x180 픽셀, iOS용)

### 생성 방법
1. https://www.favicon-generator.org/ 접속
2. 원본 이미지 업로드 (최소 260x260px 권장)
3. 생성된 파일들을 다운로드
4. 위 경로에 정확한 이름으로 업로드

---

## 🏢 2. 로고 이미지 - 필수

### 깃허브 위치
```
저장소 루트/
└── assets/
    └── images/
        └── logo/
            └── horizontal-logo.png    ← 여기에!
```

### 파일명 (정확히 이 이름으로!)
- `horizontal-logo.png` (영문 파일명 사용 권장)

**중요**: 
- `assets/images/logo/` 폴더 안에 넣어야 함
- 한글 파일명 대신 영문 파일명 사용 (깃허브 호환성)
- 현재 코드에서 `/assets/images/logo/horizontal-logo.png`로 참조하고 있음

### 사용 위치
- 헤더(상단 네비게이션) 로고
- 푸터(하단) 로고

---

## 🎨 3. Since 2017 배너 이미지

### 깃허브 위치
```
저장소 루트/
└── assets/
    └── images/
        └── since-2017-banner.png    ← 여기에!
```

### 파일명 (정확히 이 이름으로!)
- `since-2017-banner.png`

### 사용 위치
- 메인 페이지 히어로 섹션 상단

---

## 📂 전체 폴더 구조 (깃허브 저장소)

```
your-repo/
├── index.html
├── 가로형-로고.png                    ← 로고 (루트에)
├── assets/
│   └── images/
│       ├── favicon.ico                ← 파비콘
│       ├── favicon-32x32.png          ← 파비콘
│       ├── favicon-16x16.png          ← 파비콘
│       ├── apple-touch-icon.png       ← 파비콘
│       ├── since-2017-banner.png     ← 배너
│       ├── logo/                      ← 추가 로고 (선택)
│       ├── programs/                  ← 프로그램 이미지
│       ├── members/                   ← 회원 프로필
│       ├── reviews/                   ← 후기 이미지
│       ├── seminars/                  ← 세미나 이미지
│       ├── food/                      ← 맛집 이미지
│       └── backgrounds/               ← 배경 이미지
└── (기타 파일들...)
```

---

## 🚀 깃허브에 업로드하는 방법

### 방법 1: 웹 인터페이스 사용 (가장 쉬움)

#### 파비콘 업로드
1. 깃허브 저장소 접속
2. `assets` 폴더 클릭
3. `images` 폴더 클릭 (없으면 생성)
4. "Add file" > "Upload files" 클릭
5. 다음 4개 파일 드래그 앤 드롭:
   - `favicon.ico`
   - `favicon-32x32.png`
   - `favicon-16x16.png`
   - `apple-touch-icon.png`
6. "Commit changes" 클릭

#### 로고 업로드
1. 깃허브 저장소에서 `assets/images/` 폴더로 이동
2. `logo` 폴더 클릭 (없으면 생성)
3. "Add file" > "Upload files" 클릭
4. `horizontal-logo.png` 파일 드래그 앤 드롭
5. "Commit changes" 클릭

#### 배너 업로드
1. `assets/images/` 폴더로 이동
2. "Add file" > "Upload files" 클릭
3. `since-2017-banner.png` 파일 드래그 앤 드롭
4. "Commit changes" 클릭

### 방법 2: Git 명령어 사용

```bash
# 저장소 클론
git clone https://github.com/your-username/your-repo.git
cd your-repo

# 파비콘 추가
# (파비콘 파일들을 assets/images/ 폴더에 복사한 후)
git add assets/images/favicon.ico
git add assets/images/favicon-32x32.png
git add assets/images/favicon-16x16.png
git add assets/images/apple-touch-icon.png

# 로고 추가
# (horizontal-logo.png를 assets/images/logo/ 폴더에 복사한 후)
git add assets/images/logo/horizontal-logo.png

# 배너 추가
git add assets/images/since-2017-banner.png

# 커밋
git commit -m "Add favicon, logo and banner images"

# 푸시
git push origin main
```

---

## ✅ 체크리스트

업로드 전 확인사항:

### 파비콘
- [ ] `assets/images/favicon.ico` 파일 존재
- [ ] `assets/images/favicon-32x32.png` 파일 존재
- [ ] `assets/images/favicon-16x16.png` 파일 존재
- [ ] `assets/images/apple-touch-icon.png` 파일 존재

### 로고
- [ ] `assets/images/logo/horizontal-logo.png` 파일 존재
- [ ] 파일명이 정확히 `horizontal-logo.png`임 (대소문자 확인)

### 배너
- [ ] `assets/images/since-2017-banner.png` 파일 존재

---

## 🔍 현재 코드에서 참조하는 경로

### 파비콘
```html
<!-- index.html의 <head> 섹션 -->
<link rel="icon" type="image/x-icon" href="/assets/images/favicon.ico">
<link rel="icon" type="image/png" sizes="32x32" href="/assets/images/favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="/assets/images/favicon-16x16.png">
<link rel="apple-touch-icon" sizes="180x180" href="/assets/images/apple-touch-icon.png">
```

### 로고
```html
<!-- 헤더와 푸터에서 -->
<img src="/assets/images/logo/horizontal-logo.png" alt="부산청년사업가들" />
```

### 배너
```html
<!-- 히어로 섹션에서 -->
<img src="./assets/images/since-2017-banner.png" alt="Since 2017" />
```

---

## 📝 중요 사항

1. **파일명 정확히 맞추기**
   - `horizontal-logo.png` (영문 파일명 사용 - 한글 파일명은 깃허브에서 문제 발생 가능)
   - 대소문자 구분 (favicon.ico는 소문자)

2. **경로 정확히 맞추기**
   - 로고는 **assets/images/logo/** 안에 (`/assets/images/logo/horizontal-logo.png`)
   - 파비콘은 **assets/images/** 안에 (`/assets/images/favicon.ico`)

3. **GitHub Pages 사용 시**
   - 저장소 Settings > Pages에서 활성화
   - 이미지 URL: `https://username.github.io/repo-name/assets/images/favicon.ico`
   - 로고 URL: `https://username.github.io/repo-name/assets/images/logo/horizontal-logo.png`

4. **로컬 테스트 시**
   - `assets/images/logo/horizontal-logo.png` 파일이 있어야 함
   - `assets/images/` 폴더에 파비콘 파일들 있어야 함

---

## 🆘 문제 해결

### 이미지가 안 보이는 경우

1. **파일명 확인**
   - 영문 파일명 사용 (`horizontal-logo.png`)
   - 한글 파일명은 깃허브에서 URL 인코딩 문제 발생 가능
   - 대소문자 정확한지 확인

2. **경로 확인**
   - 로고는 `assets/images/logo/` 안에 있는지 확인
   - 파비콘은 `assets/images/` 안에 있는지 확인
   - 절대 경로 사용 (`/assets/images/...`)

3. **GitHub Pages 확인**
   - Settings > Pages에서 활성화되어 있는지 확인
   - 브랜치가 `main`으로 설정되어 있는지 확인

4. **브라우저 캐시**
   - Ctrl+Shift+Delete로 캐시 삭제
   - Ctrl+F5로 하드 리프레시

---

## 📦 예시: 깃허브 저장소 구조

```
부청사-홈페이지/
├── index.html
├── assets/
│   ├── css/
│   ├── js/
│   └── images/
│       ├── favicon.ico               ✅ 여기!
│       ├── favicon-32x32.png         ✅ 여기!
│       ├── favicon-16x16.png         ✅ 여기!
│       ├── apple-touch-icon.png      ✅ 여기!
│       ├── since-2017-banner.png    ✅ 여기!
│       └── logo/
│           └── horizontal-logo.png   ✅ 여기!
└── README.md
```

이 구조대로 파일을 넣으면 됩니다!

